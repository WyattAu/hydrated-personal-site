name: Smoke

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo "smoke ok"
      - run: bun --version
      - run: node --version
